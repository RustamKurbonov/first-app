let initialState = {
    friends: [
        { id: 0, name: 'Alexey' },
        { id: 1, name: 'Andrey' },
        { id: 2, name: 'Dima' }
     ]
}

const sitebarReducer = (state = initialState, action) => {
    return state;
}

export default sitebarReducer;